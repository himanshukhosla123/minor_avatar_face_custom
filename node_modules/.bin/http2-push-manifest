#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../http2-push-manifest/bin/http2-push-manifest" "$@"
  ret=$?
else 
  node  "$basedir/../http2-push-manifest/bin/http2-push-manifest" "$@"
  ret=$?
fi
exit $ret
